package Trees;

public class VerticalOrderTraversal extends BuildTree{
    public static void main(String[] args) {
        TreeNode root=null;
        root=buildTree(root);
        verticalOrderTraversal(root);
    }

    private static void verticalOrderTraversal(TreeNode root) {
    }
}
